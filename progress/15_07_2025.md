# 📅 End of Day Progress Report - 15 July 2025

> ✅ **Goal**: Set up foundational user authentication system with secure registration and login

---

## 📌 What Was Completed Today

### 🔐 1. Role Management Setup
- ✅ Created `Role.java` enum with:
    - `TEAM`: Regular bidding team
    - `ADMIN`: Admin user who controls auction
- 📂 Package: `com.Auction.Auction_website.Enums`

---

### 👤 2. User Entity & Repository
- ✅ Created `User.java` entity with fields:
    - `id`, `name`, `email`, `password`, `role`
- ✅ Table mapped as `users` (to avoid conflict with SQL `user`)
- ✅ Used `@Enumerated(EnumType.STRING)` for clean role storage
- 📂 Package: `com.Auction.Auction_website.Entity`

- ✅ Created `UserRepo.java` with:
  ```java
  Optional<User> findByEmail(String email);
